#include <stdio.h>

/**
 * main - its the main function that serve as the starting point
 *
 * Return: 0 when successful
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
